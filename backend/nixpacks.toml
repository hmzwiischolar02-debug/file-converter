[phases.setup]
nixPkgs = ["python312", "libgcc", "zlib", "libjpeg", "libpng", "freetype"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
