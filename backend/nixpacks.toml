[phases.setup]
nixPkgs = ["python312", "python312Packages.pip", "libgcc", "zlib", "libjpeg", "libpng", "freetype"]

[phases.install]
cmds = ["python3.12 -m pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"